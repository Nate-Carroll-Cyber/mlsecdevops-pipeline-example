# Python dependencies for GAIPS materials evaluation pipeline.
# Keep this file present — CI setup, pip-audit, rag-smoke-eval, and
# guardrail-regression all require it to exist even when the scripts
# themselves use only the standard library.

pandas>=2.0.0
requests>=2.31.0
jinja2>=3.1.0
