# Terraform — never commit real values, state, or provider binaries.
# State can contain secret material (KV values, tokens); tfvars holds your
# cluster URL and claim scopes. Keep terraform.tfvars.example tracked.
terraform.tfvars
*.auto.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
