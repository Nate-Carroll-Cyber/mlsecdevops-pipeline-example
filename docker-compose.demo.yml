# Minimal local demo stack for Counter-Spy.ai.
# This is meant for laptop demos and smoke testing, not a production deployment.
#
# Topology: postgres + otel-collector(+jaeger) + counter-spy-sam-spade-service
#           + counter-spy-backend (the gateway — serves the server-rendered
#           analyst console AND the /v1 API on :8080) + counter-spy-ctf-frontend
#           (the standalone noir CTF surface). The gateway 404s
#           /v1/ctf/sam-spade/* now; the CTF frontend talks to the sam-spade
#           service directly via its own proxy.
# The analyst console is at http://localhost:18080/  (no separate frontend container).
services:
  counter-spy-backend:
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
      args:
        # Baked into the analyst-console bundle at build time. VITE_BACKEND_BEARER_TOKEN
        # must equal this service's runtime INTERCEPT_BEARER_TOKEN (from .env.demo.local).
        VITE_BACKEND_BEARER_TOKEN: "${VITE_BACKEND_BEARER_TOKEN:-local-demo-backend-token-123456}"
        VITE_CTF_FRONTEND_URL: "http://localhost:3001"
        VITE_OTEL_EXPORTER_OTLP_ENDPOINT: "http://localhost:4318"
    container_name: counter-spy-backend
    env_file:
      # Local-only demo secrets and provider creds. This file is gitignored.
      - .env.demo.local
    environment:
      # Core backend runtime settings.
      APP_PORT: 8080
      APP_ENV: dev
      LOG_LEVEL: info
      # Instruction similarity monitor backed by PostgreSQL + pgvector.
      INSTRUCTION_MONITOR_ENABLED: "true"
      INSTRUCTION_MONITOR_EMBEDDING_DIMENSIONS: "${INSTRUCTION_MONITOR_EMBEDDING_DIMENSIONS:-768}"
      INSTRUCTION_MONITOR_EMBEDDINGS_ENABLED: "${INSTRUCTION_MONITOR_EMBEDDINGS_ENABLED:-true}"
      INSTRUCTION_MONITOR_EMBEDDINGS_API_BASE_URL: "${INSTRUCTION_MONITOR_EMBEDDINGS_API_BASE_URL:-http://192.168.0.183:11434/v1}"
      INSTRUCTION_MONITOR_EMBEDDINGS_MODEL_ID: "${INSTRUCTION_MONITOR_EMBEDDINGS_MODEL_ID:-nomic-embed-text}"
      INSTRUCTION_MONITOR_EMBEDDINGS_MAX_CHUNKS: "${INSTRUCTION_MONITOR_EMBEDDINGS_MAX_CHUNKS:-4}"
      INSTRUCTION_MONITOR_SEED_CORE_ON_START: "${INSTRUCTION_MONITOR_SEED_CORE_ON_START:-true}"
      # Default safeguard judge for local inspector-style review.
      SAFEGUARDS_API_BASE_URL: "http://192.168.0.183:1234/v1/chat/completions"
      SAFEGUARDS_MODEL_ID: "gpt-oss-safeguard-20b"
      SAFEGUARDS_TIMEOUT_MS: "30000"
      # The CTF surface lives in its own container at :18120. The gateway no
      # longer proxies /v1/ctf/sam-spade/*; the CTF frontend talks to the
      # sam-spade-service directly via its own proxy. The gateway only
      # persists CTF review artifacts (durable across restarts).
      SAM_SPADE_ENABLED: "true"
      CTF_REVIEW_ARTIFACTS_STORE_PATH: "/app/data/ctf-review-artifacts.db"
      # OpenTelemetry: export traces/metrics/logs over OTLP/HTTP to the collector.
      OTEL_SERVICE_NAME: "counter-spy-backend"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4318"
      OTEL_EXPORTER_OTLP_PROTOCOL: "http/json"
      OTEL_TRACES_SAMPLER: "${OTEL_TRACES_SAMPLER:-parentbased_traceidratio}"
      OTEL_TRACES_SAMPLER_ARG: "${OTEL_TRACES_SAMPLER_ARG:-1.0}"
      OTEL_LOG_LEVEL: "${OTEL_LOG_LEVEL:-warn}"
      # Lara Translate credentials come from the local env file above so the
      # demo stack stays stable across rebuilds and restarts.
    ports:
      - "18080:8080"
    volumes:
      # Durable CTF review-artifact store.
      - counter_spy_gateway_data:/app/data
    depends_on:
      counter-spy-postgres:
        condition: service_healthy
      otel-collector:
        condition: service_started
    restart: unless-stopped

  # Standalone Sam Spade CTF service. Its own image built from
  # services/sam-spade/Dockerfile — only /healthz and /v1/ctf/sam-spade/* are
  # served (on SAM_SPADE_SERVICE_PORT). It owns the SQLite session store
  # volume and makes its own safeguard/responder calls.
  counter-spy-sam-spade-service:
    build:
      context: .
      dockerfile: services/sam-spade/Dockerfile
    container_name: counter-spy-sam-spade-service
    env_file:
      - .env.demo.local
    environment:
      APP_ENV: dev
      LOG_LEVEL: info
      SAM_SPADE_ENABLED: "true"
      SAM_SPADE_DEFAULT_CASE_ID: "case-067"
      SAM_SPADE_STORE_PATH: "/app/data/sam-spade.db"
      SAM_SPADE_SERVICE_PORT: "18120"
      SAFEGUARDS_API_BASE_URL: "http://192.168.0.183:1234/v1/chat/completions"
      SAFEGUARDS_MODEL_ID: "gpt-oss-safeguard-20b"
      SAFEGUARDS_TIMEOUT_MS: "30000"
      OTEL_SERVICE_NAME: "counter-spy-sam-spade-service"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4318"
      OTEL_EXPORTER_OTLP_PROTOCOL: "http/json"
      OTEL_TRACES_SAMPLER: "${OTEL_TRACES_SAMPLER:-parentbased_traceidratio}"
      OTEL_TRACES_SAMPLER_ARG: "${OTEL_TRACES_SAMPLER_ARG:-1.0}"
      OTEL_LOG_LEVEL: "${OTEL_LOG_LEVEL:-warn}"
    ports:
      - "18120:18120"
    volumes:
      # Sam Spade session data lives with the service that owns it now.
      - sam_spade_data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:18120/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    depends_on:
      otel-collector:
        condition: service_started
    restart: unless-stopped

  # The analyst console is no longer a separate container — the gateway
  # (counter-spy-backend) server-renders it and serves the built assets on :8080.
  # Open http://localhost:18080/ ; its /v1/* calls are same-origin.

  # Standalone Sam Spade CTF frontend (noir "Case 067" surface). Talks to the
  # Counter-Spy CTF UI. Talks directly to the standalone sam-spade-service for
  # /v1/ctf/sam-spade/* (so the CTF stays alive if the gateway is offline) and
  # posts each turn's review artifact to the gateway's /v1/ctf/review-artifacts.
  # NOTE: this still runs the Vite dev server in-container (Dockerfile.ctf-frontend);
  # converting it to a server-rendered/static build is a follow-up.
  counter-spy-ctf-frontend:
    build:
      context: .
      dockerfile: Dockerfile.ctf-frontend
      args:
        # Baked into the CTF iframe's production bundle at build time.
        # VITE_BACKEND_BEARER_TOKEN must equal both services' runtime INTERCEPT_BEARER_TOKEN
        # (the gateway and sam-spade-service share the bearer for now).
        VITE_BACKEND_BEARER_TOKEN: "${VITE_BACKEND_BEARER_TOKEN:-local-demo-backend-token-123456}"
        VITE_OTEL_EXPORTER_OTLP_ENDPOINT: "http://localhost:4318"
    container_name: counter-spy-ctf-frontend
    environment:
      # Runtime config: vite preview routes /v1/ctf/sam-spade/* + /healthz to the
      # sam-spade-service and /v1/ctf/review-artifacts to the gateway. See
      # ctf-frontend/vite.config.ts for the proxy table.
      SAM_SPADE_PROXY_TARGET: "http://counter-spy-sam-spade-service:18120"
      BACKEND_PROXY_TARGET: "http://counter-spy-backend:8080"
    ports:
      - "3001:3001"
    depends_on:
      - counter-spy-backend
      - counter-spy-sam-spade-service
    restart: unless-stopped

  counter-spy-postgres:
    image: pgvector/pgvector:pg16
    container_name: counter-spy-postgres
    environment:
      POSTGRES_DB: counter_spy
      POSTGRES_USER: counter_spy
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env.demo.local and run docker compose with --env-file .env.demo.local}"
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_INITDB_ARGS: "--auth-host=scram-sha-256 --auth-local=scram-sha-256 --data-checksums"
    command:
      - "postgres"
      - "-c"
      - "password_encryption=scram-sha-256"
      - "-c"
      - "log_connections=on"
      - "-c"
      - "log_disconnections=on"
      - "-c"
      - "log_statement=ddl"
      - "-c"
      - "log_min_duration_statement=250"
      - "-c"
      - "statement_timeout=10s"
      - "-c"
      - "max_connections=100"
    ports:
      - "127.0.0.1:15432:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U counter_spy -d counter_spy"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  # OpenTelemetry Collector: OTLP in (4317 gRPC / 4318 HTTP), out to stdout debug,
  # Prometheus (:8889), and Jaeger. See otel/collector-config.yaml.
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.117.0
    container_name: counter-spy-otel-collector
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    volumes:
      - ./otel/collector-config.yaml:/etc/otelcol-contrib/config.yaml:ro
    ports:
      - "127.0.0.1:4317:4317"   # OTLP gRPC
      - "127.0.0.1:4318:4318"   # OTLP HTTP
      - "127.0.0.1:8889:8889"   # Prometheus scrape endpoint (/metrics)
    depends_on:
      - jaeger
    restart: unless-stopped

  # Jaeger all-in-one for viewing traces at http://localhost:16686.
  jaeger:
    image: jaegertracing/all-in-one:1.62.0
    container_name: counter-spy-jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "127.0.0.1:16686:16686"  # Jaeger UI
    restart: unless-stopped

volumes:
  # Named volume used by the standalone Sam Spade service's SQLite session store.
  sam_spade_data:
  # Named volume for the gateway's durable CTF review-artifact store.
  counter_spy_gateway_data:
